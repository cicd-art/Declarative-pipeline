pipeline {
    agent any
    
    
    environment {
        PATH = "/root/mvn_jenkins/github-maven-example/example:$PATH"
    }
    
    stages {
        stage("Git Checkout")
        {
            steps{
                git 'https://github.com/Sachiniraveni/Sachiniraveni.git'
            }
        }

        stage ("maven build")
        {
            steps {
                
                sh "mvn clean package"
            }
        }
    }
}
